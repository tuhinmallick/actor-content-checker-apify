{
  "title": "Store Content Checker Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "urls": {
      "title": "URLs to Monitor",
      "type": "string",
      "description": "URLs to monitor for content changes (one URL per line)",
      "editor": "textarea",
      "prefill": "https://example.com\nhttps://www.apify.com/change-log"
    },
    "contentSelector": {
      "title": "Content Selector",
      "type": "string",
      "description": "CSS selector for the content to monitor (e.g., '.main-content', '#article', 'article')",
      "editor": "textfield",
      "prefill": ".main-content"
    },
    "screenshotSelector": {
      "title": "Screenshot Selector",
      "type": "string",
      "description": "CSS selector for screenshot capture (optional, defaults to contentSelector)",
      "editor": "textfield",
      "prefill": ".main-content"
    },
    "sendNotificationText": {
      "title": "Notification Text",
      "type": "string",
      "description": "Custom text to include in notifications (optional)",
      "editor": "textfield",
      "prefill": "Content changed!"
    },
    "sendNotificationTo": {
      "title": "Email Address",
      "type": "string",
      "description": "Email address to send notifications to when content changes",
      "editor": "textfield",
      "prefill": "your-email@example.com"
    },
    "informOnError": {
      "title": "Inform on Error",
      "type": "string",
      "description": "How to handle errors: 'email', 'slack', or 'none'",
      "editor": "select",
      "enum": ["email", "slack", "none"],
      "default": "email"
    },
    "navigationTimeout": {
      "title": "Navigation Timeout",
      "type": "integer",
      "description": "Timeout for page navigation in milliseconds",
      "minimum": 5000,
      "maximum": 120000,
      "default": 30000
    },
    "maxRetries": {
      "title": "Max Retries",
      "type": "integer",
      "description": "Maximum number of retries for failed requests",
      "minimum": 0,
      "maximum": 10,
      "default": 5
    },
    "retryStrategy": {
      "title": "Retry Strategy",
      "type": "string",
      "description": "When to retry failed requests",
      "editor": "select",
      "enum": ["on-block", "on-all-errors", "never-retry"],
      "default": "on-block"
    }
  },
  "required": ["urls", "contentSelector", "sendNotificationTo", "informOnError"]
}